# Base URL, WITHOUT /v1 (the client adds it per-request)
RNA_API_BASE_URL=https://apex-prod-eu-integration.eu.roadnet.com/integration

# RNA web services credentials (same ones used to log in to the Roadnet Anywhere API)
RNA_API_USERNAME=api@harash.co.il
RNA_API_PASSWORD=changeme

# Default length of a normal work day, in minutes, used for KPI calculations when a tool call doesn't override it
RNA_NORMAL_WORKDAY_MINUTES=540
